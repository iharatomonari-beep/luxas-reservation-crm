# セッション 2026-06-14

## やったこと
- `docs/instructions/_index.md` のキューを順次実行（包括承認で自走）。
- 完了化（実機確認OK）: **T001〜T029 すべて完了**。
  - 予約台帳の核心修正（Tailwind content修正でカードが時刻位置に配置）、ブース容量判定、指名トグル、基本/全体表示（コマ幅可変）、シフト色分け、行高さ圧縮、ドラッグUX（ドロップ先行ハイライト＋移動時刻バッジ）。
  - 各マスタ/画面: 店舗設定・営業時間設定画面、ブース10、スタッフ8＋2ヶ月シフト、曜日別/月間シフト、メニューカテゴリ、オプション、タグ（顧客/ルート/カルテ）、経費、クレカ/電子マネー。
  - 運用画面: 予約一覧、返客一覧、会計（支払・レジ）、日次管理、経営指標（簡易バー＋KPI）、顧客フル検索、PM準拠の台帳UI（左レール/集計バー/予約モーダル）。
- 実装途中（このセッションで着手）:
  - **T030（実行中・実機確認待ち）**: 予約データ完全化。`Reservation` 拡張（preference/bookingTagIds/optionIds/discount系/isConsecutive）。受付モーダルでこだわり/予約ルートタグ(T025)/オプション(T024)/個別・一括割引/連続を保存。施術コメントは既存 `memo` 流用。編集モーダルで再表示。lint/build OK。
  - **T031（未着手で中断）**: 営業時間設定の台帳ランタイム連動。方針＝営業開始/終了をランタイム化（slotMinutesはリスク回避で現状維持）まで決めたが、コード未変更で終了。

## 現在の状態
- T001〜T029: 完了。
- T030: 実行中（実装済み・実機確認待ち）。
- T031〜T040: 未実行（T035〜T040は本日追加された新規タスク）。

## 次にやること（優先順）
1. **T031**: ledger の `businessStart`/`businessEnd` を `useStoreSettings`（localStorage）からランタイム取得に変更。`getReservationPlacement`/`clampReservationStartMinutes` に businessStart/End を引数で渡し、ReservationCard にも props で渡す。slotMinotesは当面据え置き＝別途。
2. T032（物販）→ T033（会計×決済マスタ×集計実値）→ T034（顧客詳細充実）。
3. T035（ステータス/キャンセル管理）, T036（当日情報3タブ）, T037（割引/インターバル/連続の計算）, T038（メール管理モック）, T039（経営指標 詳細帳票）, T040（店舗設定拡張）。

## 判断・決定事項
- T030: 施術コメントは新フィールドを作らず既存 `memo` を流用。storageキー再シードは行わず（全フィールド任意＝後方互換）。
- T031: slotMinutesのランタイム化はカード配置/ドラッグ/グリッド計算の中核に波及しリスク大のため、まず営業開始/終了のみ連動する方針。
- 進め方: 以降は「1タスクずつ・確実に」（lint/build→報告→次）に切替。

## 既知の問題・注意点
- **ツール書式ミスで度々停止**: 私（Claude Code）が誤った呼び出し書式を使うと無実行＝停止に見える。`_execution-policy.md` の自己解決ルールに「正規書式を徹底・無反応時は即再実行」を明記済み。
- ドラッグ診断ログ `[drag:start]`/`[drag:move]`/`[drag:drop]` は温存中（本番前に別タスクで削除）。
- 営業時間設定は現状まだ台帳に未連動（T031で対応予定）。
- 多数の機能で会計/物販/性別/リピート等は未実装のため「-」やダミー表示（各画面に注記）。

## 引き継ぎ（未消化）
- T030 の詳細モーダルでの読み取り表示は最小（再表示は編集モーダルで担保）。必要なら詳細表示を拡充。
- T020積み残しのうち「インターバル」挙動は T037 で計算実装予定。

---

# 追記（同日・後続セッション）: T031〜T040 を実装

未実行タスク T031〜T040 を上から順に1つずつ実装（包括承認・CAUTION）。各タスクで `npm run lint`（エラー/警告0）・`npm run build`（成功・全31ページ生成）を確認。STOP対象（削除/DB/env/認証/依存追加/指示外ファイル）には未着手。ledger を触る T031/T033/T035/T036/T037 は1つずつ・並行編集せず順次実施。**全タスク「実装済・実機確認待ち（実行中）」**。

## やったこと（タスク別）

### T031 営業時間ランタイム連動
- `reservation-ledger.tsx`: モジュール定数 `businessStart/businessEnd/slotMinutes/slotsPerHour/slotsPerQuarterHour` を撤去→コンポーネント内 `useStoreSettings()` から導出（ランタイム）。時間軸依存の純粋関数（`slotEndTime`/`getReservationPlacement`/`clampReservationStartMinutes`）へ引数で時間軸を渡し、`ReservationCard` に props 追加。`slots`/`hourSlots` の useMemo 依存も実値化。
- 当初計画では slotMinutes を据え置き予定だったが、初期値が現行と同値で後方互換のため slotMinutes も含めランタイム化（リスク低）。

### T032 物販マスタ＋販売
- 型 `RetailCategory/RetailItem/RetailSale`＋初期データ＋新規キー（再シードトークンは追加せず新規キーのみ）。`retail-item-manager.tsx`（商品＋カテゴリCRUD）/`features/sales/retail-sales.tsx`（販売登録=単価自動入力・小計/明細・合計）。`/dashboard/retail`・`/dashboard/retail-items`、sidebar 追加。

### T033 会計×決済マスタ×集計実値化
- `checkout-modal.tsx`: クレジット/電子マネー選択時に決済マスタ（T029）からブランド選択 select 追加→`cardBrand/emoneyBrand` 保存。
- `reservation-ledger.tsx`: `checkoutSummary` で集計バーの 総販売額/支払方法別/客単価/新規/リピート を実値化（新規/リピート＝過去会計済来店の有無・電話優先）。
- `analytics-reports.tsx`: 物販タブを T032 データで実値化。

### T034 顧客詳細充実
- `customer-manager.tsx`: 予約履歴から「来店サマリー」自動集計（予約総数/来店/会計済/売上合計/初回・最終/キャンセル）。履歴カードに会計・指名表示。型変更なし。

### T035 ステータス・キャンセル管理
- `reservations/types.ts`: `cancelType/cancelReason/canceledAt`＋`cancelTypeLabels`。
- `reservation-ledger.tsx`: 詳細でステータス変更（予約中/完了）＋キャンセル種別/理由/日時パネル、集計バー「無断キャンセル」実値化。
- 残: 予約一覧のキャンセル日時列は reservation-list.tsx が対象外ファイルのため別タスク化。

### T036 当日情報3タブ
- `reservation-ledger.tsx`: 台帳上部に 予約情報/販売情報/返客情報 タブ（顧客名検索・選択連動、販売=会計＋物販当日分、返客=当日キャンセル）。ドラッグ不変。

### T037 割引/インターバル/連続
- `computeReservationPricing`（四捨五入）で売上見込を受付フォーム/詳細に表示。`intervalMinutes` 追加＋`effectiveEndMinutes` で同一スタッフ重複に加算。連続予約 ON で次枠プリフィル再オープン。
- 範囲: 会計プリフィルは checkout-modal が対象外のため表示に留める。ブース容量判定（非変更ファイルの helper）はインターバル非対応。

### T038 メール管理（モック）
- `features/mail/*`・`/dashboard/mail`・sidebar。定型文CRUD/一斉配信(条件抽出→モック配信→履歴・◯日除外)/履歴取消/eDM設定。実送信・依存なし、全画面に注記。

### T039 経営指標 詳細帳票
- `analytics-detail-reports.tsx`・`/dashboard/analytics/reports`・sidebar。売上明細/クレジット会社別/日次(日別・個人別)/顧客数推移、各 CSV 出力（既存 `serializeCsv` 流用・lib追加なし）。

### T040 店舗設定拡張
- `store-settings.ts`: StoreSettings 任意拡張（業務メモ/会員番号採番/予約用HP/オンライン予約・通知/インボイス）＋初期値。
- `store-settings-manager.tsx`: 折りたたみセクション化（1フォーム1保存）＋会員番号プレビュー。実連携なし注記。T031 連動は不変。

## 実機確認の依頼（まとめて1回）
1. T031 設定変更→台帳の範囲/全体/ドラッグ/配置/シフト色分けが追従
2. T032 `/dashboard/retail-items` CRUD・`/dashboard/retail` 販売→明細/合計
3. T033 会計でブランド選択→集計バー/analytics物販が実値
4. T034 顧客詳細「来店サマリー」と履歴の会計/指名
5. T035 ステータス変更・キャンセル種別/理由/日時→無断キャンセル反映
6. T036 台帳上部3タブの表示/検索/選択連動
7. T037 売上見込・インターバル重複加算・連続予約の続け取り
8. T038 `/dashboard/mail` 定型文/対象抽出→モック配信/取消/eDM
9. T039 `/dashboard/analytics/reports` 各帳票＋CSV出力
10. T040 `/dashboard/settings` 各セクション保存・再表示（営業時間連動が不変）

## 判断・決定事項（追記分）
- 新規/リピート＝過去会計済来店有無（電話優先）。客単価＝総販売額÷会計済ユニーク客数（四捨五入）。割引％は小計基準・四捨五入。
- インターバルは同一スタッフ重複のみ反映（容量helperは非変更ファイルのため対象外）。
- メール/オンライン予約/通知/決済は実連携せずモック・設定保持のみ（STOP遵守）。

## 別タスク化候補（残課題）
- ~~予約一覧のキャンセル日時列／会計売上見込プリフィル／容量判定インターバル反映~~ → **T041 で対応済**。
- ドラッグ診断ログ `[drag:*]` の削除（実機OK後・別タスク）。

---

# 追記2: T030〜T040 完了化 ＋ T041 実装

- ユーザー実機確認OKにより **T030〜T040 を「完了」へ更新**（_index）。
- **T041（積み残し整合）を実装**（実装済・実機確認待ち）。対象ファイルのみ変更、指定外ファイルは未着手。
  1. 予約一覧 `reservation-list.tsx`: 「キャンセル日時」列を追加（cancelType≠none の canceledAt を `YYYY/MM/DD HH:mm` 表示＋種別ラベル）。colSpan 10→11。
  2. 会計モーダル `checkout-modal.tsx`: 開いたとき売上額を初期化。未会計は売上見込（T037と一致）をプリフィルし支払も同額の現金1行に。会計済の修正時は既存 saleAmount/payments を復元（従来は0リセットだった挙動を改善）。`useEffect([reservation, services, options])`。
  3. 容量判定 `hasBoothCapacity`: 判定対象の任意 `intervalMinutes`（既定0）と既存予約の `intervalMinutes` を占有時間に加算。未設定は従来どおり。
  4. 売上見込ロジックは `computeReservationPricing` を **`features/master-data/mock-data.ts` に共用helperとして集約**し checkout-modal から import（T037の台帳側コピーと同一式・小計基準/四捨五入）。台帳側コピーは T041 の対象外ファイル(`reservation-ledger.tsx`)のため今回は重複のまま温存（将来 import へ寄せる余地）。
- 検証: `npm run lint` エラー/警告0・`npm run build` 成功（全31ページ）。
- 補足: ledger からの `hasBoothCapacity` 呼び出しは候補側 interval を渡していない（ledger は T041 対象外）。既存予約側 interval は容量判定に反映されるためペア衝突は捕捉される。候補側 interval の明示反映は ledger 呼び出し更新の別タスク余地。

## 実機確認の依頼（T041）
1. 予約一覧（/dashboard/reservations/list）でキャンセル済み予約に日時＋種別が表示される。
2. 会計モーダルを開くと売上見込が売上額に入っている（手入力で上書き可・支払と一致）。会計修正時は前回額が復元。
3. インターバル付き予約があるブース種別で、インターバル時間も含めて容量が埋まる（空き無し判定になる）。

---

# 追記3: T042（予約台帳PM配置寄せ Step1）

- `reservation-ledger.tsx` のみ変更（ロジック不変）。lint エラー/警告0・build 成功。
- 削除: タイトル「Reservation Ledger / 予約台帳」見出し＋説明文、サマリカード3枚（表示日/予約数/担当数）の section。未使用化した `selectedDateLabel` と `SummaryCard` 定義も除去。
- 並び替え: ルート `<div>` を `space-y-4` → `flex flex-col gap-4` にし、各ブロックへ `order-*` を付与してPM順に。
  - order-1 ヘッダ（新規予約＋日付ナビのコンパクト行）／ order-2 StatusMessage（`empty:hidden`ラッパ）／ order-3 Debug／ **order-4 タイムラインブロック（左レール＋本体・中身不変）** ／ order-5 当日情報3タブ／ order-6 選択中／ order-7 保留棚。モーダルは fixed のため order不要。
- 判断（要報告）: タスクは「JSXの物理移動」を想定だが、タイムライン本体JSX（約390行）の物理移動は破損リスクが高いため、**CSS `order` で同一の表示順を実現**（視覚結果・ロジックは仕様どおり）。物理移動が必須なら別途対応可。
- 状態: T042 実行中（実機確認待ち）。実機確認ポイント＝タイトル/サマリが無い・タイムラインが上・3パネルが下・既存操作（新規/日付/タブ/選択/保留/ドラッグ）が従来どおり。

---

# 追記4: T043（新規予約のゲスト化＋CTI電話自動入力）

- 3ファイル変更（ロジック=必須緩和とprefill追加のみ・Reservation型変更なし）。lint エラー/警告0・build 成功。
  - `app/dashboard/reservations/new/page.tsx`: `?tel=`（無ければ`?phone=`）を電話欄、`?name=` を顧客名に prefill。
  - `features/reservations/reservation-create-page.tsx`: 顧客名/電話の `required` 撤去・必須バリデーション2ブロック削除。prefill型に customerName/phone 追加。`createInitialForm` 既定 customerName=「ゲスト」(prefill優先)・phone=prefill||空。保存時 `normalizeText(customerName) || "ゲスト"`。
  - `features/reservations/reservation-ledger.tsx`（台帳インライン）: 同様に required 撤去・必須チェック削除・openCreateForm 既定「ゲスト」・保存時ゲスト補完。編集時は従来どおり既存顧客名。
- 他の必須（メニュー/担当/日付/時刻/5分/シフト/容量）は不変。
- 状態: T043 実行中（実機確認待ち）。
- 補足: CTI側は「アロハCTIエンジンが着信時に開くURL」を `/dashboard/reservations/new?tel=<番号差込>` に設定する運用が必要。

## 実機確認の依頼（T043）
1. 台帳「新規予約」→顧客名が「ゲスト」既定・必須バッジ無し。顧客名/電話を空のままでもメニュー/担当/時刻が正しければ登録できる。
2. 専用画面 `/dashboard/reservations/new` も同様にゲスト登録できる。
3. `/dashboard/reservations/new?tel=09012345678` で電話欄に番号が入る。`&name=山田` で顧客名も入る。
4. 既存の予約編集・他必須チェックは従来どおり。

---

# 追記5: T044（ナビをPM風 上部メニューに作り替え・左サイドバー撤去）

- 新規 `components/layout/top-menu.tsx`（PM風 上部ドロップダウン）＋ `components/layout/dashboard-shell.tsx` 改修。`sidebar.tsx` は残置（未使用・削除せず）。lint エラー/警告0・build 成功。
- top-menu: 7構成（トップ／予約台帳▾／日次管理▾／店舗情報▾／顧客情報▾／商品情報▾／メール管理／経営指標▾）。`usePathname` でactive、外側クリック＆ルート変更で閉じる。PC=横並びバー＋ドロップダウン、モバイル=ハンバーガーで縦アコーディオン。`/dashboard/grid`（準備中）は載せず死にリンク回避。色は既存 luxas-green/mist 流用・lib追加なし。
- dashboard-shell: 2カラム（左サイドバー＋main）を撤去し縦積みに。ヘッダー=ブランド「LUXAS」（/dashboard）＋TopMenu（PC横並び）＋email（lg）＋ログアウト。モバイルはハンバーガーでヘッダー下に全幅アコーディオン。プレビュー注記・handleSignOut は不変。
- 状態: T044 実行中（実機確認待ち）。

## 実機確認の依頼（T044）
1. 全 `/dashboard/*` で左サイドバーが消え、上部にメニューバーが出る。
2. 各ドロップダウン（予約台帳/日次管理/店舗情報/顧客情報/商品情報/経営指標）が開き、各リンクで正しく遷移。現在ページがactive表示。
3. トップ・メール管理の単独リンクが動く。
4. モバイル幅でハンバーガー→縦メニューが開く。
5. ログアウト／プレビュー注記は従来どおり。

---

# 追記6: T045（予約台帳の表示をPMに合わせる）

- 対象は `features/reservations/reservation-ledger.tsx` の1ファイルのみ。lint エラー/警告0・build 成功。計算ロジック・予約カード・ドラッグ処理は不変（集計バーは位置移動のみ）。
- (1) 上部日付バー: 既存の `<前日 / 今日へ戻る / 日付input / 翌日>` に「再読込」ボタン（RotateCw・`window.location.reload()`）を追加。右側に静的プレースホルダ（天気/すべて▾/店舗▾・title="準備中"）＋現在時刻（HH:mm・JST・useState＋useEffectで1分ごと更新）を追加。「新規予約」は残置。
- (2) 表示切替タブ: 既存「基本/全体」（機能維持）に「シフト/ブース/両方」をdisabled（title="準備中"・クリックで落ちない）で追加し5タブ化。
- (3) 予約集計バー: タイムライン section を `flex flex-col` 化し、集計バー div に `order-last`＋`border-t` を付与してタイムライン下部（下部ツールバーの下）へ移動。中身・計算は不変。
- (4) ドラッグ用横長帯: タイムライン上部（タブ直下）に説明帯「日付をまたいで予定を変更したい場合は、一旦このエリア（下部の保留棚）に置いてください。」＋右に「シフト追加」リンク（→ /dashboard/shifts/monthly）。ドラッグ結線は重いため、タスク許容どおり「説明帯＋保留棚誘導＋シフト追加リンク」に留めた（既存の保留棚セクションは残置）。
- 状態: T045 完了（実機確認OK 2026-06-14）。

## 実機確認の依頼（T045）
1. 台帳上部に `<前日 / 今日へ戻る / 日付 / 翌日 / 再読込>`・現在時刻が並ぶ（天気/すべて/店舗はダミー）。
2. 表示切替が5タブ（基本/全体は機能、シフト/ブース/両方は無効・押しても落ちない）。
3. 予約集計の数値がタイムラインの下に表示される。
4. タイムライン上部にドラッグ用の横長帯＋「シフト追加」リンクがある。
5. 既存操作（日付移動・新規予約・予約クリック・ドラッグ・保留棚）が従来どおり。
