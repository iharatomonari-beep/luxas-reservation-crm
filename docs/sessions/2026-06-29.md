# セッション 2026-06-29

## やったこと
- **フェーズ4 顧客DB化 S1（ダミーのみ・★実名簿は未投入）完了**。
  - `supabase/phase4-customers.sql`：`customers` に `legacy_id` / `profile(jsonb)` 追加＋`legacy_id` 一意index＋**ダミー2件seed**（customer-001 森下彩／customer-002 神谷玲奈・架空）。冪等。ユーザーがSupabaseで実行。
  - `features/master-data/mappers/customer-mapper.ts` 新規：profile(jsonb)退避方式（staff/services同型）。コアPII列は実列、追加項目（visit/sales集計・T067拡張・各種日付）は profile へ。★空文字↔null往復ガード（birth_date は ""→null、任意列は null→undefined で原本一致）。
  - `migration-config.ts` に `luxas-customers-sample → supabase` 登録。
  - `npx tsc --noEmit` / `npm run lint` 両方グリーン。
- **Claude自身が認証ブラウザセッションのJSで実DB客観検証**：
  - REST取得：customers 2件・httpStatus 200。legacy_id/コアPII列/tags/profile（firstVisitDate・lastVisitDate）/store_id・tenant_id すべて正。
  - fromRow往復：customer-001 が初期データ（initialCustomers[0]）と**完全一致**（churn無し）。
  - 編集→保存→再読込（RLS書込）：chart_memo を PATCH→200・永続化OK→**元へ復元**（テスト残骸なし）。
- 検証のため停止していた dev サーバー（`npm run dev`）を起動（バックグラウンド・:3000）。

## 現在の状態
- DB化済み：コレクション30＋設定2＋**顧客（S1・ダミー2件）**。組織は案A（未移行）。
- 顧客：器・mapper・切替・往復・RLS書込まで動作確認済み。**ただし§5未充足のため実名簿(PM)は未投入**。

## §6 オーナー判断（2026-06-28 確定・反映済み）
1. 着手＝S1。2. 参照範囲＝テナント全員（既存RLSのまま・変更なし）。
3. 型⇔列ギャップ＝profile(jsonb)退避。customer_notes 当面未使用、caution/chartMemo は顧客レコード保持。
4. #6バックアップ・#8専門家確認はオーナー並行手配（実名簿投入前に必須）。#4はS4で二重予約EXCLUDE制約の実DB適用を検証。

## 次にやること（S2以降・★ユーザー承認後に着手）
1. **S2 監査ログ基盤**（definer RPC `log_audit`＋顧客の閲覧/編集/CSV入出力の記録）。
2. S3 CSVエクスポートの owner 限定。
3. S4 RLS本番強度化（tenant_id バックフィル→NOT NULL化／二重予約EXCLUDE制約の実DB適用を検証）。
4. S5 ダミーCSV→DB取込の実証。
- ※S6バックアップ・S7専門家確認・実名簿投入はオーナー実行。

## 既知の問題・注意点
- 顧客テーブルの整数列（total_visits 等）は pilot中は未使用（既定0）。アプリの該当文字列項目は profile に保持。多店舗/本番化時に正規化要否を判断。
- customers.store_id は NOT NULL のため既定店舗（渋谷）で充足。参照範囲はテナント単位なので店舗値は解決用の固定。

## 追記: フェーズ4 S2（監査ログ基盤）完了
- `supabase/phase4-audit-rpc.sql`：definer RPC `log_audit(action_type,target_type,target_id,detail,result)`（ユーザーがSupabaseで実行）。
  - ★actor/role/tenant/store は `auth.uid()` からSQL側で解決（クライアント偽装不可）。アプリユーザーでなければ raise（anon拒否）。
  - 既存 audit_logs を使用（新テーブルなし）。audit_logs は insertポリシー無し＝**書込はdefiner経由のみ**。
  - ★PIIガード：detail はホワイトリスト7キー（count/format/fields/changedFields/source/mode/reason）のみ `jsonb_build_object` で再構築。氏名/電話/カルテ本文は構造的に保存されない。
  - target_id：顧客 legacy_id → customers.id(uuid) 解決。authenticated のみ grant。
- `remote-collection.ts` に `logAudit()`（session有り時のみ・best-effort・失敗握り潰し・PII非送信）。
- 顧客4箇所に配線：`customer-manager.tsx`（詳細閲覧=openDetail→view／saveForm→create/update〔changedFields=列名のみ〕／saveMemo→update〔caution・chartMemo〕）、`import-export-manager.tsx`（通常CSVインポート→import／PMインポート→import:peakmanager／顧客エクスポート→export〔count+列名〕）。
- `tsc`/`lint` グリーン。
- **Claude自身が実DB検証**：4箇所同一payloadでRPC（PII混入2件含む）→4件記録・全HTTP200。actor_user_id解決・actor_role='owner'（auth由来）・target_id=customer-001のuuid一致・**pii_leak_found=[]**（森下/電話/秘密の本文はどこにも残らない）。
- 注意：audit_logs は authenticated に delete ポリシー無し（append-only）。検証で reason='s2-verify' のテスト行4件が残存。不要なら下記SQLで削除可（任意・破壊的なのでユーザー実行）：
  `delete from public.audit_logs where detail->>'reason' = 's2-verify';`

## 次にやること（S3以降・★ユーザー承認後）
- S3 CSVエクスポートの owner 限定（UIガード＋owner限定の出力経路）。S4 RLS本番強度化（tenant_idバックフィル→NOT NULL／二重予約EXCLUDE制約の実DB適用検証）。S5 ダミーCSV→DB取込実証。

## 追記: フェーズ4 S3（顧客CSVエクスポートの owner 限定）完了
- `supabase/phase4-export-rpc.sql`（ユーザー実行）：
  - `app_is_owner()`（UI表示制御用・auth判定）。
  - ★`export_customers()`：owner でなければ raise（**UI迂回しても non-owner はサーバーで拒否**）。出力成功時に `log_audit('export','customer_csv',{count,fields列名,source})` を内部で必ず記録。返り値はCSV用13列。authenticated のみ grant。
- アプリ側：
  - `remote-collection.ts` に `isOwner()`（app_is_owner・session有り時のみ）／`exportCustomersViaRpc()`。
  - `import-export-manager.tsx`：顧客のエクスポートを supabase時は `export_customers()` 経由に変更（結果からCSV生成・S2のクライアント側export記録は削除＝RPC内部記録に一本化）。UIは supabase時 owner のみボタン表示（非owner は「書き出しは owner のみ」表示）。local時は従来動作。
- `tsc`/`lint` グリーン。
- **Claude自身が実DB検証**：owner export=HTTP200・2件・13列／非owner呼び出し=P0001「owner role required」・0件（owner ガードが log_audit より前で raise＝失敗試行は記録されない）／export監査記録=actor_role owner・detail{count:2,fields:13列名,source:export_customers}・PIIなし。
  - 注：専用 manager/staff アカウントが無いため、非owner拒否は「owner ロールを持たない呼び出し」で実証。認証済み manager/staff も同一ガード経路で拒否される。文字どおりの manager ログイン確認はテストログイン提供時に実施可。
  - 注：検証で export 監査行が1件増（source=export_customers・架空2件）。

## 次にやること（S4以降・★ユーザー承認後）
- S4 RLS本番強度化（tenant_id バックフィル→NOT NULL化／★二重予約EXCLUDE制約が実DBに効いているか検証）。S5 ダミーCSV→DB取込実証。

## 追記: フェーズ4 S4（RLS本番強度化）完了
- **現状把握（実DB）**：tenant_id は全テーブルで null=0／store_id は店舗スコープ表 null=0・テナント共通マスタは全行null（設計どおり）。→ バックフィル不要。
- `supabase/phase4-rls-notnull.sql`（ユーザー実行）：`tenant_id` を 40テーブルに `set not null`（1トランザクション・all-or-nothing）。★テナント共通マスタの store_id は触らない（nullable維持）。
- **#4 二重予約 EXCLUDE 制約を実DB実証**：テスト予約A=201、重複B（同store/同staff/同日重複時間）=`23P01 reservations_no_overlap_staff` で拒否。テスト行削除済み。rls.sqlは「設計段階」明記だが実DBでは制約が現に有効＝サーバー側で担保。
- **#3 顧客RLS再点検**：`tenant_id=app_user_tenant_id()`（テナント全員）。owner で2件参照可＝意図どおり。変更なし。
- **NOT NULL検証**：正常insert=201（アプリ主要insertは通る）／テナント共通マスタ store_id=null維持／tenant_id=null挿入はRLSが先に拒否(42501)＝NOT NULL発火(23502)はRLSに隠れて観測不可。NOT NULL適用はトランザクション成功で確定（SET NOT NULLは検証込み適用）。任意の確認クエリ（information_schema.columns で is_nullable=NO）をユーザーに提示。
- コード変更なし → tsc/lint は現状維持（前スライスでグリーン）。
- §5チェックリスト更新：#4・#5 を充足。残り #6 バックアップ・#7 ダミーCSV取込・#8 専門家確認。

## 次にやること（S5・★ユーザー承認後）
- S5 ダミーCSV→DB取込の実証（docs/sample-csv の架空CSVを取込画面からSupabaseへ投入し、件数・重複判定・PII列の往復を検証）。★実名簿は入れない。
- #6 バックアップ／#8 専門家確認はオーナー並行手配（実名簿投入前に必須）。

## 追記: フェーズ4 S5（ダミーCSV取込の実証）完了
- file_upload はセッション共有ファイルのみ許可（リポジトリ内CSV拒否）のため、**取込処理のデータ変換（CSV行→toCustomer→customerMapper.toRow→DB INSERT）を忠実に再現**して実DB検証（画面の内部処理と同一経路）。
- sample-csv/customers.csv の2件＋PM形式1件を投入し確認：
  - 件数=3挿入。往復（fromRow＝原本）c1/c2/pm すべて一致（profile退避含む）。
  - PM extras（newsletterEmail/rank/totalVisits）は profile に退避、peakManagerCustomerId は peak_manager_customer_id 列に。
  - dedup：同一 legacy_id 再投入＝23505（uq_customers_legacy_id）で拒否＝既存legacy_id突き合わせで重複防止。
  - import 監査：log_audit('import','customer_csv',{count:3,format:standard})・actor=owner・pii_leak=[]（氏名/電話なし）。
  - 掃除：架空3件を削除・残0件。
- 監査は append-only のため import テスト行（reason='s5-verify'）が残存。掃除は任意SQL：`delete from public.audit_logs where detail->>'reason' in ('s2-verify','s5-verify');`（export_customers の検証行は source='export_customers' で別途判断）。
- コード変更なし → tsc/lint 現状維持。
- §5チェックリスト：#7 充足。

## 追記: #6 バックアップ手順書を整備
- `docs/ops/supabase-backup-restore.md` 作成：日次バックアップ/PITR有効化手順＋復元テスト手順（PITR/論理ダンプ）＋復元後検証＋チェックリスト＋障害初動。
- ★PITR有効化と復元テストの実施はオーナー作業（実名簿投入の前提）。実施完了で §5 #6 を満たす。

## §5 チェックリスト 現況（2026-06-29）
- ✅#1 顧客DB化 / ✅#2 監査ログ / ✅#3 CSV owner限定 / ✅#4 予約可否サーバー側 / ✅#5 RLS本番強度 / ✅#7 ダミーCSV取込
- ⬜#6 バックアップ（手順書あり・★オーナーが有効化＋復元テスト実施で充足）
- ⬜#8 個人情報の専門家確認（★オーナー手配）
- ★実名簿(PM)投入は #6・#8 完了まで行わない。

## 追記: 実データ投入B（第1弾：業務マスタ staff/services/rooms）＋不具合2件の原因対応
### 原因（報告済み）
- 課題1（新規予約にメニューが一部しか出ない）／課題2（実スタッフがDBに無い）の共通原因＝**本物カタログ（mock-data.ts のPM実データ：staff59・services377・rooms10）が Supabase 未投入**。DBにはテストデータのみ（staff3「テスト」/service3「マッサテスト」/room1）。localStorageはsupabaseモードのpurgeでマスタ系が空。実カタログはgitにコミット済みで安全（独自入力分は無しと確認）。
- メニューは全て店舗紐付け（375件 storeScope:selected＋2件 all）。渋谷で出るのは82+2=84件（店舗ごとに異なるのが正しい挙動）。

### 実施（ユーザー承認のうえ）
1. テスト予約2件（ゲスト/テスト１/マッサージテスト）削除 → テスト staff3/service3/room1 削除。
2. currentStoreId を store-shibuya に。
3. カタログ投入（mapper経由REST・無損失）：`npx tsx scripts/_gen-catalog.ts`(一時) で mock-data を mapper.toRow 変換→public/_catalog-tmp/*.json→dev server経由でブラウザ取得→Supabase REST INSERT。staff 渋谷9／services 377／rooms 10。一時スクリプト/JSONは削除。
   - ★staff は渋谷店（homeStoreId=store-shibuya）9名のみ。他6店舗50名はコードに残置（案Bまで保留）。
   - services 377全件投入。非渋谷メニューの store_id は渋谷に寄るが、表示は profile.storeIds 基準で正しく店舗別に出る。
4. 実DB検証：DB件数 services377/staff9/rooms10。**渋谷の新規予約に出るメニュー＝84**（active && (scope!=selected || storeIds∋store-shibuya)）／**activeスタッフ9**／**ブース10**。filterMenusByStore と同一式で確認。tsc/lint green。

### 残・注意
- 他6店舗のスタッフ（50）・他店専用メニューの店舗解決は案B（多店舗化＝組織DB移行）後。コードに健在。
- 顧客（個人情報）は対象外のまま（フェーズ4・§5の#6/#8 オーナー作業待ち）。

## 追記: 多店舗対応（案B・B-min）完了
方針：org読みは localStorage 据え置き（org mapper不使用＝最小リスク）。SQLはユーザーがRun・各段で渋谷不変を照合。
- 投入前スナップショット（渋谷）：staff9/services377/rooms10/retail_sales3/register_records2/reservations0/store uuid …fcb826・code store-shibuya。
- `supabase/phaseB-stores-seed.sql`：他6店舗を stores に追加。★stores.code=アプリStore.id（store-gotanda-east 等）。渋谷行は非対象＋on conflict do nothing。→ 7店舗、storeIdByCode 全7解決、渋谷不変を確認。
- `supabase/phaseB-services-rls.sql`：services を tenant 共通RLS（select=tenant、write=owner/manager tenant）。→ 377件テナント閲覧可、渋谷84不変、各店storeIds別件数を確認。
- `supabase/phaseB-owner-allstores.sql`：owner の scope_store_id=null（全店）。→ オーナーが全59スタッフをSELECT可＝全店解決を確認。
- 他店スタッフ50名投入（mapper経由REST・一時 `scripts/_gen-staff-others.ts`＋public経由・投入後削除）：store_id を各 homeStoreId に正しく解決。計59名。store_id基準でも店舗別件数一致（RLS店舗正当性）。
- ★渋谷不変の最終照合：staff9/services377/rooms10/retail3/register2＝スナップショットと完全一致。
- 不変条件厳守：DB stores.code＝アプリStore.id（currentStoreId/records.storeId は不変）。org mapper不使用。
- ブース：渋谷のみ10（他店は未定義＝容量ベース予約で運用。必要時に定義）。
- tsc/lint green。一時スクリプト/JSON削除。顧客は対象外のまま。

### 残（案B-fullや今後）
- org（tenants/areas/stores）の完全DB化（org mapper・stores に sort_order/profile）は将来必要なら別スライス。現状 org読みは localStorage。
- 店長/スタッフの店舗限定ロール・他店ブース定義・店舗限定RLSテストは後日。

## 追記: ブースの店舗スコープ化＋全店10ブース（案B続き）
課題：rooms は店舗フィールドを持たず、hasBoothCapacity が rooms 全件を数える → 70件入れると全店（渋谷含む）が70ブース扱いになり渋谷が壊れる。
対応（アプリ改修＋DDL＋seed）：
- `ServiceRoom` に `homeStoreId?` 追加。`store-room-scope.ts` 新設（isRoomInStore/filterRoomsByStore・未設定は渋谷扱い）。
- `room-mapper`：store_id=homeStoreId解決＋profile往復（fromRowはprofile.homeStoreIdから復元）。
- 予約3画面の hasBoothCapacity 呼び出し4箇所（create×1／ledger×3：validateReservationForm・validateDraggedReservation×2／availability×1）で rooms を現在店舗に絞ってから渡す。room-manager等のマスタ表示は全件のまま。
- `supabase/phaseB-rooms-scope.sql`（ユーザーRun）：rooms に profile jsonb 追加＋既存10ブース（store_id=渋谷）に profile.homeStoreId='store-shibuya' 付与（本体不変）。
- 他6店舗へ各10ブース計60をREST投入（legacy_id=room-<storecode>-N・store_id/homeStoreId 正・localStorage非破壊）。
- 実DB照合：rooms総数70、store_id基準＝各店10、homeStoreId基準（容量判定が見る数）＝各店10、★渋谷 active 10で不変。tsc/lint green。
- 残：他店ブースの名称/種別の個別調整・room-manager のマスタ表示を店舗別にするかは後日（必要時）。

## 追記: 多店舗化 事前点検（①〜④）＋本番ビルド起動
- ①★shifts=0（S4時80→0）。原因：テストスタッフ3名削除時に on delete cascade で「テスト staff のシフト80件」が連鎖削除。カタログ59名にはシフト未作成。→ タイムラインは shift基準（getVisibleStaffForSelectedDate）のため、全7店（渋谷含む）でスタッフが出ない。実害データ損失なし（80はテスト分・実シフトは mock generateSeedShifts に健在＝未投入）。★シフト用意は提案のみで停止（大量生成しない）。
- ②分離：取引データは store_id 付き。現状 渋谷のみ（register2/retail3）、他6店0。RLS（store_id in app_user_store_ids）＋画面 filterReservationsByStore/filterRecordsByStore で分離。ダミー予約（渋谷/中目黒）で店舗別クエリの分離を実証→後始末済（reservations 0）。
- ③予約データ経路：各店で reservations 投入→store_id解決→店舗別取得→削除 まで正常（tenant_id必須に注意）。ただしUIフォームはシフト必須のため①未解決だと「シフトが無い」で作成不可。会計クリックスルーは本番ビルドでCowork確認。
- ④集計：analytics系（top-kpi/staff-performance/analytics-reports/detail）は currentStore で filterReservationsByStore/filterRecordsByStore＝店舗別。オーナーは店舗切替で各店を閲覧。※全店合計の単一ビューは現状なし（各店別）。
- 本番ビルド：npm run build 成功→npm start 起動（:3000・HTTP307=未認証はlogin。ブラウザは既存セッションで可）。Cowork可。
- ★Cowork注意：①未解決のため全店タイムラインにスタッフが出ない（空）。シフト投入方針の決定が先。

## 追記: ①シフト投入（generateSeedShifts）＋本番ビルド再起動
- mock `generateSeedShifts()` を投入。期間 2026-06-13〜08-13（約2ヶ月・今日を含む）、**計2359件**、storeId=各staffのhomeStoreId・定休日除外。
- 投入方法：tsx で initialShifts を論理JSON出力→dev server経由でブラウザ取得→shiftMapper.toRow を再現（staff_id=legacy→uuid、store_id=code→uuid、profile{staffLegacyId,storeId}）→500件×バッチでREST投入。未解決staff 0・エラー0。legacy_id重複防止・localStorage非破壊。一時ファイル削除。
- 当日(2026-06-29 月)の店舗別出勤（=台帳に並ぶ人数）：渋谷6/中目黒11/五反田東4/西4/錦糸町4/溝の口5/元町5（各店の総roster 9/19/8/5/6/7/5 から定休日を除いた実出勤）。→ ①解消。
- ③各店テスト予約：7店に1件ずつ投入→各店1件のみ（分離）→渋谷を会計(completed)化→再読込で status=completed・店舗一致で残存→後始末（reservations 0）。
- 本番ビルド：lint/build green → npm start 起動（:3000）。Cowork が各店タイムライン・予約フローを実機確認可能。
- ※顧客は対象外。shiftsは後で消せる（legacy_id like 'shift-%' で削除可）。
