# セッション 2026-06-15

## やったこと
- T041（積み残し整合: 予約一覧キャンセル日時列／会計モーダル売上見込プリフィル／インターバルをブース容量判定に反映）を実機確認OKで「完了」化。
- T044（左サイドバー撤去→PM風 上部ドロップダウンメニュー）を実機確認OKで「完了」化。
- `_index.md` を更新（T041・T044 → 完了、全件完了メモを追記）。

## 現在の状態
- 実行キュー: **T001〜T045 すべて「完了」**。未実行・実行中タスクなし。
- 前日（2026-06-14）に T045（予約台帳の表示PM化）も実機確認OKで完了済み。

## 次にやること（優先順）
1. Cowork からの新規タスク追加待ち（`docs/instructions/tasks/` ＋ `_index.md` に「未実行」で登録されたら番号順に処理）。
2. GitHub push は認証待ちで保留中（`gh auth login` 等で認証後に `git push -u origin main`）。

## 判断・決定事項
- 実機確認待ちだった T041・T044 を、ユーザーの「実行したならOK」によりまとめて完了化。

## 既知の問題・注意点
- GitHub への push は未完了（認証情報は扱わない方針のため、ユーザー側で認証が必要）。

---

# 追記: T046（予約台帳 左レール結線）

- 変更ファイル: `features/reservations/reservation-ledger.tsx` のみ。lint/build OK。
- 開く: 選択中予約の詳細モーダルを開く（未選択時は案内メッセージ・落ちない）。
- 返客: `/dashboard/reservations/returns` へ Link。
- 会計: 選択中予約の会計モーダルを開く（未選択時は案内・落ちない）。
- 顧客: 既存どおり `/dashboard/customers`。
- カード: 回数券・プリペイド未実装のため準備中のまま（コメントで別タスク明記）。
- 状態: T046 実行中（実機確認待ち）。

---

# 追記: T047（予約受付コース選択をカテゴリタブ＋一覧に）

- 変更ファイル: `features/reservations/reservation-ledger.tsx`（ReservationFormModal）＋ `features/reservations/reservation-create-page.tsx`。lint/build OK。
- メニュー選択をカテゴリ色分けタブ＋コース一覧（名前/料金¥/時間分）に変更。選択中コースはハイライト。
- 保存形式（form.serviceMenuId）・終了時刻自動計算は不変。オプション/割引/指名/連続等のUIは従来どおり。
- create-pageは旧`<select>`を撤去しタブUIに。色は既存luxasカラー流用（lib追加なし）。
- 状態: T047 実行中（実機確認待ち）。

---

# 追記: T048（予約一覧の列をPM準拠＋実値化）

- 変更ファイル: `features/reservations/reservation-list.tsx` のみ。lint/build OK。customers等は参照のみ（破壊なし）。
- 列をPM順に: ID／予約日／開始／顧客／性別／リピート／担当／コース／売上価格／会計状況／施術コメント／予約経路／キャンセル日時。
- 実値化: 性別・リピート＝顧客マスタ照合（電話優先→氏名、リピートはtotalVisits優先・無ければローカル予約集計）、売上価格＝saleAmount or 見込（computeReservationPricing）、会計状況＝paymentStatus（会計済/未会計）、予約経路＝予約ルートタグ、キャンセル日時＝canceledAt。該当なしは「-」。
- 上部に「総販売額（当日saleAmount合計）」表示＋「＋検索条件追加」ボタン（準備中）を追加。行クリック→台帳遷移は維持。
- 状態: T048 実行中（実機確認待ち）。

---

# 追記: T049（支払・レジ一覧 新規ページ）

- 追加: `app/dashboard/payments/page.tsx`＋`features/sales/payments-register.tsx`（新規）。変更: `components/layout/top-menu.tsx`（予約台帳グループに「支払・レジ」リンク追加）。lint/build OK（/dashboard/payments 生成確認）。
- 当日の会計確定（paymentStatus=paid もしくは payments あり）予約を支払方法別に表示。列: ID/予約日/開始/顧客/性別/担当/総販売額/現金/クレカ種類/クレジット/電子マネー種類/電子マネー/回数券/商品券/プリペイド/ポイント/EPARK。
- 上部に日付picker・検索・検索条件追加(準備中)・総販売額。クレカ/電子マネー種類は payments[].cardBrand/emoneyBrand を表示。
- 会計の保存形式・型は不変（参照のみ）。依存追加なし。
- 状態: T049 実行中（実機確認待ち）。

---

# 追記: T050（返客一覧の列をPM準拠＋実値化）

- 変更ファイル: `features/reservations/booking-returns.tsx` のみ。lint/build OK。
- 列をPM順に: ID／日付／開始／顧客／種別／返客理由・ステータス／コメント／予約タグ（＋LUXAS独自の担当／コースを末尾に残置）。
- 実値化: 種別=キャンセル種別ラベル（cancelType・none時は「取消」）、返客理由=cancelReason、予約タグ=予約ルートタグ名（bookingTagIds）。無ければ「-」。固定「キャンセル」表示を廃止。
- 上部に顧客名・電話の検索＋「＋検索条件追加」(準備中)を追加。期間from〜to・行クリック台帳遷移は維持。
- 状態: T050 実行中（実機確認待ち）。

---

# 追記: T051（共通スプリットパネル・マスタUIコンポーネント）

- 追加: `components/master/master-split-panel.tsx`（新規・client・generic `<T extends {id:string}>`）。lint/build OK。既存マスタ画面は未変更（差し替えはT052）。
- 公開Props: items / columns(MasterColumn<T>[]: key,header,render,className) / searchKeys / selectedId / onSelect / onCreate / renderDetail / searchPlaceholder / createLabel / emptyListLabel / emptyDetail。
- 左=名前検索＋検索条件追加(準備中)＋新規作成＋一覧（行クリック選択・選択行ハイライト）、右=明細設定（renderDetailに委譲、selectedId="" で新規モード）。既存luxasスタイル流用・lib追加なし。
- 状態: T051 完了（単体では画面変化なし）。

---

# 追記: T052（マスタのスプリットパネル化）1/7 スタッフ

- 変更ファイル: `features/master-data/staff-manager.tsx` のみ。lint/build OK。
- T051の `MasterSplitPanel` を用い、左=一覧（ID/氏名/ニックネーム/役割/表示順/状態・名前検索・新規作成・行クリック選択）＋右=明細フォームに差し替え。
- CRUD維持: 追加/更新（行選択→編集）/削除（明細内ボタン）/表示順/対応可能メニュー/有効トグル。データ構造・localStorageキーは不変。
- 選択状態: editingId（null=未選択 / ""=新規 / id=編集）で右ペイン制御。
- 状態: T052 実行中（1/7 スタッフのみ実機確認待ち。残6枚はユーザーの方針Aにより、スタッフの実機確認後に横展開）。

---

# 追記: T052（マスタのスプリットパネル化）2〜7/7

- 変更ファイル: `features/master-data/fee-master-manager.tsx`（クレカ＋電子マネー共通）/ `category-manager.tsx` / `cost-master-manager.tsx`（経費）/ `room-manager.tsx` / `features/customers/customer-manager.tsx`。lint/build OK。
- スタッフ/ブース/経費/クレカ/電子マネー/カテゴリ: 共通 `MasterSplitPanel` に統一（左一覧＝ID＋主要列＋名前検索＋新規作成＋行クリック選択、右明細＝編集フォーム＋削除）。CRUD・localStorageキー・データ構造は不変。
- 顧客: 既存が左一覧＋右詳細＋充実検索のPM準拠構成のため全面書き換えは回避（破壊リスク回避）。一覧カードに「会員番号 / ID」を追加表示し要件を充足。充実検索は維持。
- プレビュー確認: /dashboard/staff（行クリックで編集フォーム表示・CRUD動作）、/dashboard/creditcards（ID/会社名/手数料率/表示順/状態のスプリット表示）。
- 状態: T052 実行中（7/7実装済・実機確認待ち）。ブース/経費/電子マネー/カテゴリ/顧客は未巡回のためまとめて実機確認依頼。

---

# 追記: T053（商品拡充）①オンライン予約フラグ

- 変更: `features/master-data/types.ts`（ServiceMenu に `onlineBooking?: boolean` 追加・任意）/ `features/master-data/service-manager.tsx`（トグル＋一覧「オンライン予約」○×列）。option-manager は既に onlineBookable 対応済（変更不要）。lint/build OK。
- 状態: T053 実行中（①実装済・実機確認待ち）。残: ②セット商品マスタ ③物販カテゴリマスタ ④EPARK設定の新規3画面。

---

# 追記: T046 完了化

- ユーザー「OK（今確認できないから）」により T046 を「完了」化（lint/build OK・変更は reservation-ledger.tsx のみ）。実機確認は後日。問題があれば差し戻し。

---

# 追記: T047 完了化

- ユーザー「T047 OK」により完了化（lint/build OK・変更は reservation-ledger.tsx ＋ reservation-create-page.tsx）。

---

# 追記: T053（商品拡充）②③④ 新規3画面

- ②セット商品マスタ: 型 `CourseSet` 追加＋seed＋`features/master-data/course-set-manager.tsx`（MasterSplitPanel）＋`app/dashboard/course-sets/page.tsx`。列: ID/カテゴリ/セット名/施術料金/表示順/オンライン予約/状態。
- ③物販カテゴリマスタ: `RetailCategory` に `shortName?` 追加＋`retail-category-manager.tsx`＋`app/dashboard/retail-categories/page.tsx`。既存 retailCategoriesStorageKey を共有（物販商品画面と同データ）。**PM既定カテゴリへの破壊的再シードはSTOP条件のため実施せず**（既存データ維持・省略名対応のみ）。列: ID/名前/省略名/表示順/状態。
- ④EPARK掲載設定: `epark-settings.tsx`（おすすめコース1/2のドロップダウン＋保存・単一オブジェクトlocalStorage `luxas-epark-settings`）＋`app/dashboard/epark/page.tsx`。
- メニュー導線: `components/layout/top-menu.tsx` 商品情報グループに セット商品/物販カテゴリ/EPARK掲載設定 を追加。
- lint/build OK（35ページ）。状態: T053 実行中（①〜④実装済・実機確認待ち）。

---

# 追記: T053 完了化＋現況整合＋T061登録

- **T053 完了化**: ユーザー実機OK（セット商品/物販カテゴリ/EPARK表示確認OK）。`_index.md` T053→完了。
- **_index.md 現況欄を整合**（2026-06-15時点の正）:
  - 完了: T001〜T047, T051, T053。
  - 実行中（実装済・実機確認待ち）: T048 / T049 / T050 / T052(7/7)。lint/build OK。
  - 未実行: T054 / T055 / T056 / T057 / T058 / T059 / T060 / T061。次着手は T054。
  - 旧ガイダンス（T007/T018→T020等）は履歴メモ化。
- **T061 登録**: 実行キューに row61 を「未実行」で追加（`tasks/T061-org-hierarchy-types-seed.md`／外販SaaS土台＝tenant→area→store の型・初期データを非破壊追加・既存不変）。

## 整合メモ（実機確認の扱い）
- T048/T049/T050/T052 は実装・lint/build 済だが、ユーザー実機確認が未取得のため「実行中（実機確認待ち）」のまま。確認後に一括「完了」化する。

---

# 追記: T054（タグ3分割）

- 変更: `features/master-data/tag-manager.tsx`（`TagManager({kind})` 種別固定化＋MasterSplitパネル化。顧客=管理コードなし、予約ルート/施術カルテ=管理コードあり）。`app/dashboard/tags/page.tsx`→`/dashboard/customer-tags`へredirect。`components/layout/top-menu.tsx` 顧客情報グループに3項目（顧客タグ/予約ルートタグ/施術カルテタグ）。
- 新規ページ: `app/dashboard/customer-tags`・`route-tags`・`chart-tags`。既存タグデータ（kindフィールド）を活用・破壊なし・予約受付の予約タグ参照は不変。
- lint/build OK（38ページ）。状態: T054 実行中（実機確認待ち）。

---

# 追記: T055（日次分割）

- 変更: `features/daily/daily-ops.tsx`（`DailyOps({view})` 追加。view別に該当セクションのみ描画。view="all"=旧統合4タブで後方互換）。`components/layout/top-menu.tsx` 日次管理グループに6項目追加。
- 新規ルート: `app/dashboard/attendance`(出勤退勤)・`open-register`(開店)・`check-register`(レジ金点検)・`close-register`(閉店)・`close-history`(閉店処理検索=締めレジ金の日付範囲検索)・`daily-report`(売上日報)。
- 売上日報をPM全項目に拡張: 本日の結果/予算達成度/スタッフ提案数/明日の予定/実施プラン/振り返り/シフト状況/天気＋一時保存/日報送信（モック・新キー`luxas-daily-reports`）。既存の集計（来店数/件数/総売上/支払方法別）は流用。
- STOP遵守: 実際の現金確定・締め処理は作らず入力/保存のみ。会計・予約データ破壊なし。
- lint/build OK（44ページ）。状態: T055 実行中（実機確認待ち）。

---

# 追記: T056（メール5画面）

- 変更: `features/mail/mail-manager.tsx`（`MailManager({view})` 追加・5ルート対応。AutoTabに `mode`(edm/simple)絞り込み追加。`CancelTab` 新規＝配信一括停止）。`app/dashboard/mail/page.tsx`→view="history"。`components/layout/top-menu.tsx` メール管理を単独リンクから5項目グループに変更。
- 新規ルート: `app/dashboard/mail/cancel`(配信一括停止)・`templates`(定型文)・`edm`(eDM詳細)・`edm-simple`(シンプルeDM)。配信履歴は /dashboard/mail（一斉配信＋履歴）。
- 実送信なしモック（記録のみ）。既存メールデータ・型は不変（status/isSimple流用）。依存追加なし。
- lint/build OK（48ページ）。状態: T056 実行中（実機確認待ち）。

---

# 追記: T057（経営指標拡充）

- 変更: `features/analytics/analytics-detail-reports.tsx` のみ。/dashboard/analytics/reports の詳細帳票を9タブに拡充。
- 追加タブ: スタッフ別売上／商品別売上／物販売上（retailSales×スナップ単価）／時間帯別来店（開始時刻の時で集計）／帳票出力（店舗売上日報・予約明細・時間別来店・個人実績・顧客数推移・返客一覧へのリンク集）。既存（売上明細/クレカ別/日次/顧客数推移）と合わせ計9タブ。
- 各帳票はCSV出力（既存 serializeCsv 流用・グラフlib等の依存追加なし＝STOP遵守）。物販/会計未整備は0件+注記。
- メニューは既存「経営指標→詳細帳票」内のタブ拡充のため新規ルートなし。lint/build OK（48ページ）。状態: T057 実行中（実機確認待ち）。

---

# 追記: T058（店舗設定拡張）

- 変更: `features/master-data/store-settings.ts`（StoreSettingsにPM基本情報17項目を任意追加＋初期値）、`features/store-settings/store-settings-manager.tsx`（「基本情報」セクション追加＝企業ID/エリアID/店舗ID/店舗コード/店舗名/略称/郵便番号/都道府県/市区町村番地/住所2/電話/FAX/メール/HP URL/担当部署/担当者名/背景色。利用可能クレカ会社一覧/電子マネー一覧＝マスタ参照の読取表示。店舗サマリ/ウィジェット枠）。
- 既存の営業時間/会員番号/予約用HP/オンライン予約/インボイス セクションは維持。台帳が参照する営業時間キー（businessStartTime等）は不変＝連動維持。外部連携/決済/認証は未実装（STOP遵守）。
- lint/build OK（48ページ）。状態: T058 実行中（実機確認待ち）。

---

# 追記: T059（残り新規画面5種）

- 追加コンポーネント: `features/store-ops/`（online-blocks / shift-templates / shift-pattern-manager / data-errors-manager / users-master）＋ルート `app/dashboard/`（online-blocks / shift-templates / shift-patterns / data-errors / users）。
- 本日のオンライン設定: 開店閉店/公開状況/シフト状況カード＋オンラインブロック(日付検索＋新規/削除・列 ID/日付/名前/ブロックID/開始/終了)。
- シフトひな型: 名前検索＋列(ID/ひな型名/登録スタッフ/登録シフト/予約端末/表示順・表示のみ)。
- シフトパターン: MasterSplitPanel CRUD(ID/名前/シフト時間/表示順)。
- データ不備のお知らせ: MasterSplitPanel 一覧＋右明細(表示のみ・ID/お知らせ/発生日時/分類/詳細)。
- ユーザマスタ: 表示のみ(ID/ユーザ名/ログインID/表示順/最終ログイン)。認証/権限/パスワードは非接触(STOP遵守)。
- menu: 予約台帳グループに3項目、店舗情報グループに2項目。新規localStorageキー(luxas-shift-patterns/shift-templates/data-errors/users/online-blocks)。
- lint/build OK（53ページ）。状態: T059 実行中（実機確認待ち）。

---

# 追記: T060（ナビ最終化）

- 変更: `components/layout/top-menu.tsx` のみ。PM店舗8グループ構成に整合（予約台帳/日次管理/店舗情報/顧客情報/メール管理/商品情報/経営指標＋トップ）。
- 調整: データ不備のお知らせ→予約台帳グループへ移動、ユーザマスタ→「ユーザ情報」として店舗情報グループ、月間目標入力（→/shifts/monthly）を日次管理に追加、本日のオンライン設定/シフトひな型/シフトパターンを予約台帳グループに整列。
- 全46メニューリンクが実在ルートに対応することをスクリプトで検証（死にリンク0）。経営指標の各帳票は /analytics/reports のタブとして提供（個別ルートは作らずリンク集で誘導）。
- lint/build OK（53ページ）。状態: T060 実行中（実機確認待ち）。

---

# 追記: T061（org階層の型・初期データ）＋全体現況

- 追加: `features/org/types.ts`(Tenant/Area/Store)・`features/org/mock-data.ts`(tenants/areas/storesキー＋初期データ 株式会社東邦→東京→LUXAS渋谷＋currentStoreId="store-shibuya")・`features/org/index.ts`(再エクスポート)。既存型/データ/画面は一切不変・強制FK追加なし。UIなしのため実機確認不要→完了化。
- lint/build OK（53ページ）。

## 全体現況（2026-06-15 終了時点）
- 完了: T001〜T047, T051, T053, T061。
- 実装済・実機確認待ち（実行中）: T048/T049/T050/T052/T054/T055/T056/T057/T058/T059/T060。
- **未実行タスクなし**（T046〜T061すべて実装完了）。残りはユーザーの一括実機確認のみ。
- 53ページ・lint/build エラー0・上部メニュー死にリンク0。

---

# 追記: T062（org管理画面＋店舗切替）

- 依存T061（features/org/）実装済を確認後に着手。
- 追加: `app/dashboard/org/page.tsx`＋`features/org/org-admin.tsx`（テナント/エリア/店舗の3タブCRUD・各MasterSplitPanel。エリアは所属テナント選択、店舗は所属テナント＋エリア選択。保存はT061キー luxas-tenants/areas/stores）。`features/org/use-current-store.ts`（現在店舗フック・キー luxas-current-store-id・初期値 store-shibuya）。`components/layout/store-switcher.tsx`（上部バーの店舗切替＝現在テナント/エリア表示＋storesドロップダウン）。
- 変更: `components/layout/dashboard-shell.tsx`（ヘッダーに StoreSwitcher 追加）、`components/layout/top-menu.tsx`（店舗情報グループに「組織管理」リンク）。features/org/types.ts に任意フィールド code?/plan?/sortOrder? 追加（非破壊）。
- **既存の予約/顧客/会計データは未scope（非破壊）**＝店舗を切り替えても既存画面の表示は変わらない（scopeはT063）。切替UIにその旨をtitle注記。
- 死にリンク0（/dashboard/org 実在をスクリプト確認）。lint/build OK（54ページ）。状態: T062 実行中（実機確認待ち）。

---

# 追記: T062 完了化

- ユーザー実機確認OKにより T062 を「完了（実機確認OK 2026-06-15）」に更新。`_index.md` row62→完了、現況欄も更新。
- コード変更なし・他タスク未着手。組織管理(/dashboard/org)・店舗切替UI・現在店舗保持は実装済（既存データは未scope=非破壊）。

---

# 追記: T063（予約を店舗scope・非破壊）

- 追加: `features/reservations/store-scope.ts`（`isReservationInStore`/`filterReservationsByStore`＝安全フィルタ。既定店舗 store-shibuya）。
- 型: `features/reservations/types.ts` の Reservation に `storeId?: string`（任意）追加。既存予約は無付与のまま有効。
- 新規付与: `reservation-ledger.tsx`（saveReservation 新規作成時）／`reservation-create-page.tsx`（nextReservation）に `storeId = currentStoreId` を付与。編集は既存値を spread で保持。
- 安全フィルタ適用（読み取りのみ・非破壊）: 台帳 `reservation-ledger.tsx`（normalizedReservations をフィルタ＝全表示が現在店舗基準）／`reservation-list.tsx`／`booking-returns.tsx`／支払レジ `features/sales/payments-register.tsx`／経営指標 `analytics-detail-reports.tsx`・`analytics-reports.tsx`・`top-kpi.tsx`。
- 非対象（変更なし）: 顧客/スタッフ/メニュー/ブース/商品等のマスタ、会計の保存構造、daily-ops・customer-manager・import-export（予約まわり画面に限定）。
- 検証: lint/build OK（54ページ）。プレビュー実機: 既定店舗(渋谷)で 2026-06-11 の既存予約 **10件すべて表示**／他店舗(store-osaka)に切替で **0件（予約はありません）**／渋谷へ戻すと **10件復帰**（既存データ非破壊・切替は可逆）。
- 状態: T063 実行中（実機確認待ち）。

---

# 追記: T063 完了化

- ユーザー実機確認OK（渋谷で既存予約表示・他店舗で分離・渋谷へ戻すと復帰）により T063 を「完了（実機確認OK 2026-06-15）」に更新。`_index.md` row63→完了、現況欄も更新。
- T064以降には未着手（ユーザー指示により停止）。次段階候補: T064（顧客/スタッフ/メニュー等マスタの店舗・テナントscope設計・未登録）。
