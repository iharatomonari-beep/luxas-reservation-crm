# セッション 2026-06-13

## やったこと
- `docs/instructions/_index.md` のキューを順次実行（T1〜）。

### T1: 予約枠ドラッグ移動の静的確認（実装変更なし）
- ドラッグ関連関数を静的確認: `processPointerMove`（L458）/ `handleTimelinePointerUp`（L507）/ `validateDraggedReservation`（L1546）/ `getVisibleStaffForSelectedDate`（L204）/ `beginReservationDrag`（L770）/ `clampReservationStartMinutes`（L1530）/ `reservationOverlapsMinutes`（L1480）。
- 仕様照合結果: **基本的に仕様と一致**。
  - 横移動＝開始時刻変更（5分スナップ・幅不変）: 一致。`Math.round(movedPixels / slotWidth) * slotMinutes` を `originStartMinutes` に加算、幅は `durationMinutes` 固定。
  - 縦移動＝担当スタッフ変更: 一致。`elementFromPoint`→`closest("[data-staff-id]").dataset.staffId` で行判定。
  - 重複ブロック（同一スタッフ or 同一ブース＋時間重複）: 一致。さらに `reservationOverlapsMinutes`（L1486）でキャンセル済み予約を除外し誤ブロックなし。
  - 別日移動はドラッグ不可（保留棚経由）: 一致。ドラッグは `startTime/endTime/staffId` のみ更新し `date` 不変。
  - 表示は当日有効シフトのスタッフのみ: 一致。
- 座標変換の確認:
  - 横方向は**差分（delta）方式**（`nextPointerX - pointerStartX`）のため `scrollLeft`/`getBoundingClientRect` は不要。ファイル内で両者の使用は0件＝欠落ではなく設計上の不使用。
  - 縦方向は `elementFromPoint`（ビューポート座標）でスクロール非依存。正しい。
- あやしい点（軽微・1件）: **ドラッグ最中に横スクロールするとズレる**エッジケース。ドラッグ端の自動スクロール未実装のため実用影響は小。仕様未定義につき「確認が必要」事項として留置（修正せず）。
- 診断ログ `[drag:start]`/`[drag:move]`/`[drag:drop]` はそのまま温存（実機確認後に別タスクで削除予定）。
- 検証: `npm run lint` エラー0・警告0 / `npm run build` 成功・型エラー0（全14ページ生成）。
- コード変更なし。

### T002: 店舗設定マスタ導入（方式A・ユーザー承認済み）
- 安全レベル CAUTION → 実行前確認を提示し、ユーザーが「方式A（最小変更）」で承認。
- 追加: `features/master-data/store-settings.ts`
  - 型 `StoreSettings`（businessStartTime/businessEndTime/reservationAcceptStartTime/reservationAcceptEndTime/slotMinutes）
  - `initialStoreSettings`（"10:00"/"23:00"/"10:00"/"22:00"※要確認/5）
  - localStorageキー `luxas-store-settings`
  - 単一オブジェクト用フック `useStoreSettings`（配列前提の useLocalCollection は流用せず専用実装）
- 保持方式と理由: 店舗設定は店舗ごとに1件の単一オブジェクトのため、配列前提の `useLocalCollection` を無理に流用せず、`useState`＋localStorage 読み書きの専用フックにした。欠損キーは初期値で補完。
- 参照差し替え: `features/reservations/reservation-ledger.tsx` L46-48 のハードコードを `initialStoreSettings.businessStartTime/businessEndTime/slotMinutes` から導出（方式A＝定数を中央設定の正本から導出）。`slotWidth=16` は据え置き。
- create-page: 唯一の該当箇所 L455 の `"10:00"` フォールバックは T003 対象と重なるため T002 では未変更（温存）。
- 回帰なしの根拠: 初期値が現行ハードコードと完全同値（10:00/23:00/5分）。モジュール定数経由で従来どおり全純粋関数が同じ値を参照するため、表示・挙動は不変。`slotMinutes` を設定で変えればグリッドが変わる拡張余地は確保。
- 検証: `npm run lint` エラー0 / `npm run build` 成功・型エラー0。
- 変更してはいけないファイル（customers / import-export / 既存マネージャ / lib・app / .env / package.json）は未変更。

### T004: Tailwind content に features/ 追加（最優先・緊急修正／ユーザー承認済み）
- 根本原因: `tailwind.config.ts` の `content` に `./features/**` が無く、予約UI（features配下）の `absolute`/`relative` 等のCSSが生成されていなかった。→ 予約カードが `position:static` のままインラインで左に固まり、時刻位置に並ばなかった（5/11からの設定不備）。
- 修正: `tailwind.config.ts` の `content` に `"./features/**/*.{js,ts,jsx,tsx,mdx}"` を1行追加。
- 検証: `npm run lint` エラー0 / `npm run build` 成功・型エラー0。dev再起動後ブラウザ実機確認で、`.absolute`/`.relative` CSS生成・カードが `position:absolute`・浜田13:00カード(1068px)≒ヘッダー13:00(1074px)で一致を確認。画面上もカードが時刻目盛りに沿って配置されることを確認。
- 影響範囲: 設定のcontentにパス追加のみ（既存スタイルは壊れず、生成ユーティリティが増えるだけ）。

## 現在の状態
- T001: 完了（静的確認のみ、実装変更なし）。
- T002: 完了（店舗設定マスタ導入・方式A）。
- T004: 完了（Tailwind content に features/ 追加・カード配置の根治）。
- T003: 未実行（安全レベル CAUTION）。

### T006: 初期シフトデータの休憩時間を空にする（CAUTION・ユーザー承認済み）
- 目的: 「選択スタッフの休憩時間と重なっています」で予約のドラッグ・作成が止まる問題を緩和。デフォルトで休憩なしに。
- 変更: `features/master-data/mock-data.ts` の `initialShifts` 全12件の `breakStart`/`breakEnd` を `""` に変更（種類別に一括置換）。休憩チェックのロジック・型 `StaffShift`・`shift-manager.tsx` のUIは未変更（休憩機能は将来用に温存）。
- 検証: `breakStart: ""` 12件確認、`npm run lint` エラー0 / `npm run build` 成功・型エラー0。
- 変更は `mock-data.ts` のみに収まっている。

**既存 localStorage への反映手順（必須）**: 既存ブラウザには旧休憩が `luxas-master-shifts-v2` に残るため、初期データを空にしても表示はすぐ変わらない。反映するには次のいずれか：
- DevTools の Console で `localStorage.removeItem("luxas-master-shifts-v2")` を実行して再読み込み（他のサンプルも初期化するなら `localStorage.clear()`）。
- または シフト管理画面で各シフトの休憩を空にして保存。

### T008: ブースを計10に拡張（CAUTION・ユーザー承認済み）
- 変更: `features/master-data/mock-data.ts` の `initialRooms` を2件→10件に拡張。
  - 施術ブース8: room-001「ブース1」(既存id維持・表示名変更), room-003「ブース2」, room-004「ブース3」, room-005「ブース4」, room-006「ブース5」, room-007「ブース6」, room-008「ブース7」, room-009「ブース8」。
  - 個室2: room-002「個室1」(既存id維持・表示名変更), room-010「個室2」。
- 予約の `roomId` 調整: **不要だった**。初期予約24件を分析した結果、同一 `roomId`・同日・時間重複は元から存在しなかった（重複エラーはドラッグで同じブースに重ねた時に発生していたもの）。`reservations/mock-data.ts` は未変更。
- 既存ブースの id（room-001/room-002）は維持したため、既存予約の `roomId` 参照は壊れていない（表示名のみ変更）。
- 検証: 施術8件確認、`npm run lint` エラー0 / `npm run build` 成功・型エラー0。変更は `master-data/mock-data.ts` のみ。
- **localStorage リセット手順**: 既存ブラウザには旧ブース2件が `luxas-master-rooms` に残るため、DevTools Console で `localStorage.removeItem("luxas-master-rooms")` を実行して再読み込み（全初期化なら `localStorage.clear()`）。

### T010: メニュー「個室必須」フラグ＋ブース容量判定ヘルパ（CAUTION・ユーザー承認済み）
- 変更ファイル: `features/master-data/types.ts`, `features/master-data/mock-data.ts`。
- `ServiceMenu` に `requiresPrivateRoom` を追加。**承認時は必須(boolean)だったが、必須にすると変更不可の `service-manager.tsx`（ServiceMenu構築・L94に該当フィールド無し）が型エラーになるため、ファイル制約を守って任意(`?: boolean`、未設定=false扱い)に変更**（機能は同一）。
- `initialServices` の2メニュー（ボディケア/フェイシャル）とも `requiresPrivateRoom: false`。**個室必須メニューは判断材料が無いため全 false。どれを個室必須にするか要確認（未決）**。
- 容量判定ヘルパ `hasBoothCapacity()` を `mock-data.ts` に追加・エクスポート（許可ファイル内に配置）。仕様: 入力=対象予約(date/start/end/serviceMenuId)・当日全予約・services・rooms・除外ID。メニューの requiresPrivateRoom で必要種別(private/treatment)を決め、時間が重なる同種予約数 < その種別のブース台数 なら true。**この時点では未接続（接続は T011）**。
- 既存の予約作成・台帳の判定ロジックは未変更＝挙動不変。
- 検証: `npm run lint` エラー0 / `npm run build` 成功・型エラー0。
- 補足: 途中で types.ts を必須のまま放置した瞬間にdevコンパイルが壊れ「ブラウザがテキストだけ」になったが、任意化で解消。

### 追加対応: メニュー管理画面に「個室必須」設定UIを追加（ユーザー依頼・CAUTION）
- 背景: 「個室必須メニューは今後あるので、メニュー追加時に設定できるように」との依頼。
- 変更ファイル: `features/master-data/service-manager.tsx`。
  - `ServiceForm` 型・`emptyForm` に `requiresPrivateRoom` 追加。
  - 保存 payload・編集復元(handleEdit)に `requiresPrivateRoom` を反映（編集時は `?? false`）。
  - フォームに「個室必須メニュー」トグル（`ToggleField`）を「有効にする」の上に追加。
  - メニュー一覧に「個室」列を追加（個室必須=「個室必須」/それ以外=「—」）。
- これで新規/編集メニューで個室必須を設定でき、T010 で追加した `requiresPrivateRoom` に保存される。
- 検証: `npm run lint` エラー0 / `npm run build` 成功・型エラー0。

### 追加対応: メニュー管理画面に「個室必須」設定UI（ユーザー依頼・CAUTION）
- `features/master-data/service-manager.tsx` にフォームの「個室必須メニュー」トグル＋一覧「個室」列を追加。`requiresPrivateRoom` を保存・編集復元に反映。lint/build OK。

### T011: 予約のブース固定を撤去→容量ベース判定（CAUTION・実機確認OKで完了）
- 方針: ユーザーが「推奨どおり実行（roomId温存）」を選択。
- `features/reservations/reservation-ledger.tsx`:
  - `hasBoothCapacity` を import。種別ラベル `getBoothKindLabel(serviceMenuId)` 追加（個室必須→「個室」/それ以外→「施術ブース」）。`getRoomName` は未使用化のため削除。
  - `validateReservationForm`: ブース必須/停止中チェックと `options` 引数を撤去。重複判定を「スタッフ重複のみブロック＋ブースは `hasBoothCapacity` で種別台数判定」に置換。
  - `validateDraggedReservation`: 「ブースが見つかりません」撤去。`roomId` 重複条件を外しスタッフ重複のみ＋容量判定に置換。
  - 予約詳細・カード・サマリ・フォームの「ブース名」表示を「ブース種別」に変更。フォームのブース選択(FormSelect)を種別の読み取り専用表示に置換。未使用化した `activeRooms`/`formRoomOptions`/`buildSelectableRooms` を削除。
- `features/reservations/reservation-create-page.tsx`:
  - 同様に `hasBoothCapacity` import、ブース選択UI撤去→種別読み取り専用表示、サマリ種別化、ブース必須/停止中チェック撤去、重複判定をスタッフ重複＋容量判定に置換（`validateReservationForm` に `currentServices` 引数追加・呼び出し更新）。未使用の `activeRooms`/`roomOptions`/`buildSelectableRooms` 削除。
- スタッフ重複・シフト外チェックは従来どおり維持。`Reservation.roomId` は型から削除せず温存（判定に使わないだけ）。
- 検証: `npm run lint` エラー0 / `npm run build` 成功・型エラー0。**ユーザー実機確認OK**（ブース手動選択が無い／ドラッグ移動できる／容量超過時のみ不可）。

### T012: rooms storage キーを v2 化＋自動再シード（Cowork直接修正で完了）
- `roomsStorageKey="luxas-master-rooms-v2"`、`local-storage.ts` に `SEED_RESET_TOKENS` 機構追加（古いlocalStorageを初期データへ強制再シード）。lint/build OK。

### T013: 仮スタッフ8人＋2ヶ月分シフト生成（CAUTION・承認済み）
- `features/master-data/mock-data.ts`: `initialStaff` を8人（staff-001..008、既存 001/002 の id維持・全員isActive・両メニュー対応）。`initialShifts` を生成関数 `generateSeedShifts()` で作成（`SHIFT_STAFF_PLAN` に各自の9時間枠と定休2曜日を定義）。期間 2026-06-13〜2026-08-13。
- `features/master-data/local-storage.ts`: `SEED_RESET_TOKENS` に `luxas-master-staff`/`luxas-master-shifts-v2` を追加。
- 検証: 総シフト**355件**。サンプル土06-13=7人・日06-14=7人・月06-15=5人＝設計どおり（土7日7・平日5〜6、各自週5）。lint/build OK。実機確認待ち。

### T009: シフト時間の色分け（CAUTION・実装済み／実機確認待ち）
- `reservation-ledger.tsx`：各スタッフ行の各5分セルを、その日のシフト窓内かで判定。シフト内=白、シフト外=`bg-stone-200`（当初bg-stone-100→ユーザー要望で濃く）。シフト外はクリック不可（openCreateForm呼ばず・「＋」非表示）。lint/build OK。

### T007: タイムライン「基本/全体」切替（CAUTION・実装済み／実機確認待ち）
- 判断: 全体表示=「画面幅に収める（コマ幅可変・本格対応）」をユーザー選択。
- `reservation-ledger.tsx`：`timelineView`("basic"|"full") 状態＋ResizeObserverでビューポート幅計測。`cellWidth`＝基本16px／全体=（幅-スタッフ列)/コマ数でフィット。`slotWidth`直接参照を`cellWidth`に置換（timelineWidth/ヘッダー/セル/scroll/ドラッグ/`getReservationPlacement`に`cellWidth`引数追加・ReservationCardに`cellWidth`prop）。ヘッダーに基本/全体トグル追加。lint/build OK。

### T003: 「10時ずれ」根治（CAUTION・方針A・実装済み／実機確認待ち）
- 固定`"10:00"`を`initialStoreSettings.reservationAcceptStartTime`に置換。`reservation-ledger.tsx`の`openCreateForm`、`reservation-create-page.tsx`の`createInitialForm`（import追加）。prefillありは渡された時刻を維持。lint/build OK。

### T017: 指名機能（CAUTION・実装済み／実機確認待ち）
- `reservations/types.ts`：`Reservation.nominatedStaffId?: string` 追加。
- `reservation-ledger.tsx`：`setNomination()`（設定時はstaffIdを指名者に合わせる/解除）、予約詳細モーダルに指名バッジ＋スタッフ選択＋解除UI。`validateDraggedReservation`に「指名予約は担当変更不可（nominatedStaffId≠移動先staffIdでブロック）」追加（時刻移動は可）。lint/build OK。

## 現在の実装状況（2026-06-14 時点）
- 完了（実機確認済み）: T001,T002,T004,T006,T008,T010,T011,T012,T013,T016。
- 実装済み・**実機確認待ち**: T003,T007,T009,T017（すべて lint/build 通過）。
- 未実行: T005（ドラッグUX仕上げ・ledger）, T014（営業時間設定画面・別ファイル）, T015（曜日別シフト画面・別ファイル）, T018（台帳UIをPM配置・**T007/T009/T017の実機確認完了後**）。

### T014: 営業時間設定画面（CAUTION・実装済み／実機確認待ち）
- 新規 `app/dashboard/settings/page.tsx`、`features/store-settings/store-settings-manager.tsx`。`useStoreSettings`(T002) で営業時間/受付/時間単位を編集・保存（検証付き）。
- 連動注意: 台帳は `initialStoreSettings` 定数参照のため、保存値の全体表示への即連動には台帳側を `useStoreSettings` 参照へ切替える別対応が必要（T014 範囲外）。lint/build OK。

### T015: シフト画面を曜日別パターン編集に作り直し（CAUTION・実装済み／実機確認待ち）
- `features/master-data/shift-manager.tsx` 全面作り直し。スタッフ選択＋月〜日の出勤チェック＋時刻。「今日から2ヶ月分を作成」で per-date 生成し、対象スタッフの期間内を上書き（他スタッフ・期間外は保持）。
- 週次パターン型は新設せず（types.ts 不変）。パターンは既存シフトから復元（その曜日の最初の勤務時間を採用）。休憩なし。lint/build OK。

### T018/T019/T020: 予約台帳のPM準拠作り込み（CAUTION・実装済／実機確認待ち）
- T018: 左縦アクションレール（予約=新規予約／顧客=/dashboard/customers をリンク、開く/返客/会計/カードは準備中・無効）＋下部ツールバー（カテゴリ絞り込み=機能／予約集計▼・インターバル・ひな型登録/読込=準備中無効）。`reservation-ledger.tsx`。
- T019: 予約集計バー。実値=予約中/完了/取消/総来店(顧客名ユニーク)/施術件数。ダミー=目標/総販売額/売上内訳/支払方法別/新規/リピート/客単価（会計未実装・新規/リピート/客単価の定義は要確認の注記）。
- T020: 予約作成モーダルのコース選択をカテゴリ色分けグループ一覧（機能・`categoryColorClass`）に変更。こだわり/予約タグ/個別・一括割引/連続/インターバルはPM準拠の枠（未保存）。メモを「施術コメント/メモ」に。
  - 制約報告: `reservations/types.ts` は変更不可のため、こだわり/割引/連続/予約タグの**保存は未対応**（型追加が必要）。予約タグ/オプションは**マスタ未実装**。保存対応は別タスク（型追加＋マスタ新設）として要起票。
- いずれも lint/build OK・実機確認待ち。

### T021: 予約一覧画面（CAUTION・実装済／実機確認待ち）
- 新規 `app/dashboard/reservations/list/page.tsx`、`features/reservations/reservation-list.tsx`。`components/layout/sidebar.tsx` に「予約一覧」リンク追加。
- 日付ピッカー＋顧客名/電話検索、開始時刻順テーブル。列=開始/顧客/性別/リピート/担当(指名表示)/コース/会計/メモ有無/予約経路/状態。性別・リピート・会計・経路は対応データ無しで「-」（要確認注記）。行クリックで台帳の該当日へ遷移。
- lint/build OK。

### T005, T022〜T029 一括実装（包括承認・全て実機確認待ち）
- **T005**: ドラッグUX＝ドロップ先スタッフ行ハイライト＋移動中の時刻バッジ（reservation-ledger.tsx）。
- **T022 会計**: `Reservation` に paymentStatus/saleAmount/payments[] 追加。`checkout-modal.tsx` 新規。予約詳細に会計ボタン＋会計状況表示。推奨構造で実装。
- **T023 月間シフト**: `/dashboard/shifts/monthly`（日×スタッフ出勤チェック＋時刻・即保存、日次目標 新キー luxas-daily-targets）。
- **T024 商品マスタ**: カテゴリ管理 `/dashboard/categories`＋オプション管理 `/dashboard/options`（型 MenuCategory/ServiceOption・初期・CRUD）。既存 service.category 文字列と並存。
- **T025 タグ＋検索**: タグマスタ `/dashboard/tags`（顧客/ルート/カルテ・種別切替CRUD）＋顧客フル検索 `/dashboard/customers/search`（多条件・新規/customer-manager不変）。
- **T026 返客一覧**: `/dashboard/reservations/returns`（status=canceled流用・期間絞込）。
- **T027 日次管理**: `/dashboard/daily`＝出勤退勤打刻/レジ金(開店/点検/閉店・金種テーブル)/売上日報(会計集計)/経費登録(前月コピー)＋経費マスタ `/dashboard/expense-accounts`。
- **T028 経営指標**: `/dashboard/analytics`（スタッフ別/商品別売上・時間帯別来店・顧客数推移の簡易バー）＋トップKPIカード。**グラフlibは追加せず**(STOP方針)。物販は枠のみ。
- **T029 決済マスタ**: クレカ会社 `/dashboard/creditcards`＋電子マネー `/dashboard/emoney`（共通FeeMasterManager・CRUD）。ユーザ/認証/TORICOMは未着手(STOP遵守)。
- 全タスク lint/build OK。sidebar に各導線追加。package.json/auth/supabase/.env は未変更。
- **未保存・要マスタ化の残**: 予約のこだわり/割引/連続/予約タグの保存(型追加)、物販マスタ、返客区分マスタ、会計の支払選択肢とクレカ/電子マネーマスタの結合。

## 注意・残課題
- 実装済み・**実機確認待ち**: T003,T007,T009,T017(旧版),T014,T015。
- **T017 は改訂版**（担当スタッフ必須を撤去→指名ボタンに置換）に変更されたが、私の実装は旧版（担当選択を残し指名を追加）。改訂版は新規予約の既定担当の判断が必要で未着手。
- **T005**（ドラッグUX仕上げ）はお手本スクショ依存で未着手。
- **T018**（台帳UI全面PM配置）は T007/T009/T017 の実機確認完了後。
- 大量の未検証UI変更が蓄積。**ブラウザ実機確認を推奨**（特に T007 コマ幅可変＝配置/ドラッグ中核）。
- T011（ブース固定撤去→容量ベース判定へ差し替え・今の本題）, T009（シフト時間色分け）, T003（10時ずれ・方針A待ち）, T005（ドラッグUX仕上げ）, T007（基本/全体表示切替）が未実行。いずれも CAUTION。

## 次にやること（優先順）
1. T2: 店舗設定マスタ導入（型＋初期値、台帳/作成の参照差し替え。画面は後回し）
2. T3: 「10時ずれ」根治（startTime未渡し時のフォールバック修正。T2依存）

## 判断・決定事項
- T1 はドキュメント記載どおり実装変更せず静的確認のみで完了とした。

## 既知の問題・注意点
- ドラッグ診断ログが残存（本番前に別タスクで要削除）。
- 台帳の営業時間が `businessStart/End="10:00"/"23:00"` でハードコード（T2/T3で対応予定）。
- ドラッグ中の横スクロール時の座標ズレは仕様未定義（要確認）。
