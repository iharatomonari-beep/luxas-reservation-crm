# タスクID：T038
# タスク名：メール管理一式（配信履歴/取消/定型文/eDM/シンプルeDM）※枠組み

## 安全レベル
CAUTION（新規画面群）。**実メール送信は行わない**（ローカルのモック運用）。

## 目的
PMのメール管理相当の画面枠を用意する。LUXAS v0.1では実送信せず、対象抽出・定型文・配信履歴の「記録」までのモック実装。

## 背景・注意
- catalog §1のメール管理メニュー（配信履歴/取消/定型文設定/eDM設定/シンプルeDM設定）。**実機の各画面内部は未巡回**＝本タスクはPM標準＋顧客フル検索(T025)を前提に設計。相違あれば実機再確認。
- 実際のメール送信API・外部連携は**しない**（STOP対象に触れない）。

## 対象ファイル / 変更してよいファイル
- 新規: `features/mail/*`、`app/dashboard/mail/*`、`components/layout/sidebar.tsx`
- 参照: 顧客フル検索(T025)で配信対象抽出

## 変更してはいけないファイル
- `lib/supabase/` `supabase/` `.env*` `package.json`（メール送信ライブラリ追加=STOP）
- `features/reservations/` の既存ロジック、上記以外

## 実行内容
1. メール定型文設定: 定型文のCRUD（件名/本文/種別）。localStorage。
2. 一斉配信(対象抽出): フル検索条件で対象顧客を抽出→「配信(モック)」で配信履歴に記録（実送信しない・「送信はモック」明記）。配信制限(○日以内除外)。
3. 配信履歴: 日時/対象数/定型文/種別。配信取消=履歴のキャンセル記録。
4. eDM/シンプルeDM設定: 自動配信ルールの設定枠（トリガ/定型文/対象）。実行はモック。
5. 全画面に「v0.1は実送信なし・記録のみ」の注記。

## 検証方法
- `npm run lint` / `npm run build`／実機でユーザー確認。

## 完了条件
1. 定型文CRUD・対象抽出→モック配信→履歴記録ができる。2. 既存を壊さない。3. lint/build OK。4. sessions追記・_index更新。5. 実機OKで完了。

## 停止条件
- 実メール送信/外部API/依存追加が必要になったらSTOP（モックに留める）。画面内部仕様が不明な点は推奨(PM標準)で進め「実機再確認」と報告。指定外ファイルはSTOP。lint/build失敗で停止。

## 完了後の報告内容
- 完了ID/追加画面/モック範囲/要実機再確認点/lint・build/_index更新/実機確認依頼
