# タスクID：T018
# タスク名：予約台帳PM準拠 作り込み①＝左縦アクションレール＋下部ツールバーの配置

## 安全レベル
CAUTION（予約台帳のUI変更。実行前にユーザー確認）

## 位置づけ（PM準拠 作り込みの分割索引）
予約台帳をPeakManager準拠に仕上げる作業は次に分割する（同一ファイルを触るので**1つずつ順に**）:
- **T018（本タスク）**: 左縦アクションレール＋下部ツールバーの配置
- T019: 予約集計バー（当日集計＋支払方法別内訳）
- T020: 予約受付モーダルのPM準拠化（カテゴリ色分けタブ＋オプション/こだわり/予約タグ/コメント/インターバル/割引/連続）
- （将来）T021: シフト/ブース/両方 の表示切替タブ

参照: `~/Desktop/pm_スクリーンショット/LUXAS_docs/peakmanager-feature-catalog.md` の §2（予約受付＝/Orders）。

## 前提（依存・衝突回避）
`reservation-ledger.tsx` を編集する。**T007・T009・T017 が「完了」になってから着手**。未完了なら「先行タスク未完のため保留」と報告して停止。

## 目的（本タスク）
PMの予約台帳と同じ位置に、左の縦アクションレールと下部ツールバーを置く。配色・文言・アイコンはコピーせずLUXASトーンで配置のみ合わせる。

## 対象ファイル
- `features/reservations/reservation-ledger.tsx`
- 必要なら `features/reservations/` に小コンポーネント新規追加

## 変更してよいファイル
- `features/reservations/reservation-ledger.tsx`
- `features/reservations/` 配下の新規UIコンポーネント

## 変更してはいけないファイル
- `features/reservations/reservation-create-page.tsx`
- `features/customers/` `features/import-export/` `features/master-data/`
- `lib/supabase/` `supabase/` `app/` `.env*` `package.json` `tailwind.config.ts`
- 上記「変更してよいファイル」以外

## 実行内容
1. タイムライン左外側に**縦アクションレール**（アイコン＋ラベル）を置く: 予約 / 開く / 返客 / 会計 / 顧客 / カード。
   - リンク先（既存があるものだけ繋ぐ。無いものは無効ボタン＋「準備中」ツールチップで可）:
     - 予約=新規予約導線（既存の openCreateForm 等）／顧客=`/dashboard/customers`／会計・返客・開く・カード=現状無ければ無効でOK。
2. タイムライン直下に**下部ツールバー**（横並び）を置く: カテゴリ別 絞り込み / 予約集計▼ / インターバル:ON / ひな型登録 / ひな型読込。
   - 機能まで作るのは「カテゴリ別絞り込み」だけでよい（メニューのカテゴリでカード表示を絞る。カテゴリが無ければ枠のみ）。
   - 予約集計▼=T019で中身、ひな型=将来。無効/プレースホルダで可。インターバルON=既存トグルがあれば連動。
3. 既存のドラッグ・クリック・色分け・基本/全体・指名を壊さない。

## 検証方法
- `npm run lint`（エラー0）/ `npm run build`（型エラー0）
- 実機（`npm run dev`）でユーザー確認。

## 完了条件
1. 左縦レール・下部ツールバーがPMと同じ位置関係で表示される。
2. 既存機能が壊れていない。
3. lint/build が通った。
4. `docs/sessions/` 追記、`_index.md` の T018 更新。
5. ユーザーの実機確認OKで「完了」。

## 停止条件
- T007/T009/T017 未完了なら保留・停止。
- 実行前確認（CAUTION）でユーザー許可まで着手しない。
- 各ボタンのリンク先や挙動で迷ったら停止して選択肢提示。
- 指定外ファイル変更が必要なら STOP。実機確認まで「完了」にしない。lint/build 失敗で停止。

## 完了後の報告内容
- 完了タスクID(T018) / 追加したレール・ツールバーと各ボタンの結線状況（有効/無効） / 変更ファイル / lint・build結果 / _index更新 / 実機確認依頼
