# タスクID：T049
# タスク名：支払・レジ一覧 画面を新規作成（PM /Bookings/index_pay 相当）

## 安全レベル
CAUTION（新規ページ追加。会計データ参照）

## 目的
PMの「支払・レジ」一覧に相当する画面をLUXASに新設する。

## PMの仕様（予約施術ヘッダ情報）
- 上部: 日付picker + 検索 + 検索条件追加 + 総販売額表示
- 列: ID / 予約日 / 開始時間 / 顧客名 / 性別 / 総販売額 / 現金支払 / クレジットカード種類 / クレジット支払 / 電子マネー種類 / 電子マネー支払 / 回数券利用 / 商品券利用 / プリペイド利用 / ポイント利用 / EPARKサービス

## 対象ファイル / 追加してよい
- app/dashboard/payments/page.tsx（新規。ルート名は /dashboard/payments）
- features/sales/ もしくは features/reservations/ 配下に一覧コンポーネント新規（既存の会計データ checkout/payments[] を参照）
- components/layout/top-menu.tsx（予約台帳グループに「支払・レジ」リンク追加）

## 実行内容
- 当日（日付picker）の会計済データを集計し、上記列で表示。支払方法別（現金/クレジット/電子マネー/回数券/商品券/プリペイド/ポイント/EPARK）に内訳。
- 会計種類（クレカ会社/電子マネー種類）はマスタ(creditcards/emoney)と結合。
- 総販売額を上部に表示。検索条件追加は最低限でよい。
- データは既存の予約の payments[]/saleAmount を使う（新しい保存形式は作らない）。

## 変更してはいけないファイル
- 会計の保存ロジック・型は変更しない（参照のみ）。

## 検証
- npm run lint / npm run build 成功。実機: /dashboard/payments で当日会計が支払方法別に並ぶ。メニューからも開ける。

## 完了条件
1. 画面新設＋メニュー導線。2. 会計データを支払方法別表示。3. lint/build OK。4. _index更新＋sessions追記。5. 実機OKで完了。

## 停止条件
- 会計データ構造の変更が必要なら停止して報告。依存追加はSTOP。

## 報告
- 完了ID T049 / 追加・変更ファイル / lint・build / 実機確認依頼。
