# タスクID：T046
# タスク名：予約台帳 左レール（開く/返客/会計/顧客/カード）をPM準拠で結線

## 安全レベル
CAUTION（reservation-ledger.tsx のレール部分の結線。既存画面/モーダルへ繋ぐだけ）

## 目的
左縦アクションレールの準備中ボタンを、PMのパネル対応に沿って既存のLUXAS画面/モーダルへ結線する。

## PMの各ボタンが開くもの
- 予約=予約入力 / 開く=予約詳細 / 返客=返客登録 / 会計=会計（料金・割引・支払方法） / 顧客=顧客情報 / カード=回数券・プリペイド管理

## 対象ファイル
- features/reservations/reservation-ledger.tsx のみ

## 実行内容（レール配列の enabled/onClick/href を変更）
- 予約: 既存どおり。
- 開く: 選択中予約の詳細モーダルを開く。未選択なら案内（落ちない）。
- 返客: /dashboard/reservations/returns へ Link。
- 会計: 選択中予約の会計モーダルを開く。未選択なら案内。
- 顧客: 既存どおり /dashboard/customers。
- カード: 回数券・プリペイド管理は未実装のため title="準備中" のまま。コメントで別タスクと明記。

## 変更してはいけないファイル
- 上記以外。

## 検証
- npm run lint / npm run build 成功。実機で各ボタン動作・未選択時に落ちない。

## 完了条件
1. 4ボタン結線（カードは準備中明示）。2. 既存維持。3. lint/build OK。4. _index更新＋sessions追記。5. 実機OKで完了。

## 停止条件
- 指定外ファイル変更が必要なら STOP。lint/build失敗で直せなければ停止。

## 報告
- 完了ID T046 / 変更ファイル / lint・build / 各ボタン結線先 / 実機確認依頼。
