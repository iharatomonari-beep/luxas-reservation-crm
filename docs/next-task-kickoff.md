# 次タスクのキックオフ指示文（新しいセッションの最初に貼る）

このファイルの下のコードブロックをコピペし、【今回やりたいこと】だけ書き換えて使う。

```
LUXAS 予約・顧客管理システムの続きです。まず状況を引き継いでから作業します。

■ プロジェクト
- 場所: /Users/C.C/claude code/luxas-reservation-crm（Next.js + TypeScript + Supabase）
- git: 最新コミット（この文を書いた時点で d221138 以降）、origin/main と同期・未コミットなしが基本。作業前に git status で確認。
- 動作確認は開発モード（npm run dev）で。ブラウザは command+R で反映。
- コミットは「普通のターミナル」で実行（Claude Codeのログインが切れることがある／.git/index.lock が残ったら他のgit処理が無いのを確認して rm -f .git/index.lock）。

■ 最初に必ず読む（読んだら現状を3〜5行で要約してから着手）
1. CLAUDE.md（運用ルール）
2. docs/sessions/ の最新ファイル（直近セッションの作業内容）
3. docs/design/feature-toggle-layer.md（外販向け「機能オン/オフ層」設計＋§9レビュー＋§9.7 RLS/RPC監査ゲート）
4. docs/引き継ぎ書-現在地-2026-06-30.md

■ いまの到達点（要点）
- 完了: 自動更新(固定3秒・UIなし)／新規予約のルートタグ／顧客マージ(owner限定RPC＋ソフトマージ、統合済みは一覧非表示)／重複検出ポップ(電話・メール・氏名で「同一人物では？」を顧客登録・予約受付で表示)／日次集計(店舗向け)ページ／ヘッダー整理。
- 顧客の本番化ゲート未了: #6 バックアップ有効化＋復元テスト、#8 個人情報の専門家確認。これが済むまで実顧客名簿の投入・オンライン予約の実顧客公開はしない。
- ②の実DBマージは supabase/phase4-customer-merge.sql を Supabase で適用後に有効（未適用でもクラッシュはしない）。

■ 次の候補（未着手・急ぎでない）
- 外販「機能オン/オフ層」の設計再レビュー→最小実装(P1: 機能カタログ＋メニュー出し分け)
- RLS/RPC の実効監査（外販前ゲート・§9.7の項目）
- 日次集計の第2版（税抜/税込・オンラインPC/携帯・機会損失・追加提案・健康アドバイス・RUU＝要定義）

■ 進め方のルール
- 設計や破壊的変更・SQL・外部依存追加・実顧客データは、勝手に進めず提案→承認→実装。1機能ずつ。lint/build(tsc)を必ず通す。
- ドキュメント（stale情報）より、実コードとgitの現状を正とする。

【今回やりたいこと】: （ここに具体的な依頼を書く。例：「機能オン/オフ層のP1を設計レビューして、問題なければ実装して」）
```
